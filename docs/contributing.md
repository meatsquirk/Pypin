# Contributing

See `CONTRIBUTING.md` for development workflow, code style, and PR expectations.

Common commands:

```bash
python3 -m pytest -m "not benchmark"
python3 -m ruff check .
python3 -m ruff format .
```
